import { log } from "console"

let transport :string[] = ["motorcyle","car","train","plane"]
for(let a = 0; a < transport.length; a++){
    console.log('my favourite mode of transport is' + transport[a])
};